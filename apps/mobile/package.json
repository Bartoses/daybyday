{
  "name": "@daybyday/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "echo \"no mobile tests yet\" && exit 0",
    "build": "echo \"expo build configured in EPIC 7\" && exit 0"
  },
  "dependencies": {
    "@daybyday/schemas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
