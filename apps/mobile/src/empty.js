/* global module */
// Empty stub for optional modules that Metro tries to statically resolve but
// which are never actually loaded at runtime (e.g. @opentelemetry/api inside
// @supabase/supabase-js, guarded by a .catch(() => null)).
module.exports = {};
